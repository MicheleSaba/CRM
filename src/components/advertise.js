import React from "react"

const Advertise = ({ siteTitle }) => (
  <section style={{ marginTop: "40px" }}>
    <h1>Welcome to Cedar Rapids & Marion Resale's Member Perks page!</h1>
  </section>
)

export default Advertise
